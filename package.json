{
  "name": "bitbucket-diff-themes",
  "version": "0.2.0",
  "private": true,
  "description": "Dark themes for Bitbucket Cloud PR diffs — recolors syntax + diff lines via CSS",
  "scripts": {
    "test": "node --test"
  }
}
